# Copy this file to .env before starting the server.
# The server reads these at startup via process.env.
# .env is gitignored — never commit it.

# PORT: the TCP port the Express server will listen on.
# Default: 3001
PORT=3001
